# CRAN comments

This is the initial development version of psmqte.

Before CRAN submission:

- replace placeholder maintainer information in DESCRIPTION;
- confirm package title and citation for the related paper;
- run `devtools::check()` on a machine with R and package build tools;
- review examples for runtime.
